# Discord Auto-Reply Bot

This is a Discord bot that automatically replies to messages based on configured keywords.

## Setup

1. Install dependencies: `npm install`
2. Create `.env` file from `.env.example`
3. Add your Discord bot token to `.env`
4. Run: `npm start`

## Features

- Automatic replies to specific keywords
- Configurable prefix
- Easy to extend
